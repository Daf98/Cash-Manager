<p style="color: green"><%= notice %></p>
<div>
  <%= link_to "Back", groups_path, class: "back-button main" %>
</div>
<h1 class="index-header text">Detail</h1>
<div id="trades">
<div id="trade">
  <img src="<%= @group.icon %>">  
  <h2><%= @group.name %></h2>
</div>
<div class="trade-buttons">
<% if can? :update, @group %>
  <%= link_to "Edit this group", edit_group_path(@group), class: "second" %>
  <% end %>
</div>
</div>
